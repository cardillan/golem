set shell := ["powershell.exe", "-c"]

schemacode := "mindcode cs -l -c -s -a Cardillan -w"
mindcode := "mindcode cm -l -d 3 -u source -s"

schematics:
     {{schemacode}} mandelbrot.sdf

programs:
    {{mindcode}} mandelbrot-compute.mnd
    {{mindcode}} mandelbrot-control.mnd

all:
    just schematics
    just programs
