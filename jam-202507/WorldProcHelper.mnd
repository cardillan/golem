#set target = 8w;

shard1.setprop(@copper, 120);
shard1.setprop(@lead, 90);

while true do
    do while !switch1.enabled;

    switch1.enabled = false;

    for i in 0 ... 10 do
        shard1.setprop(lookup(:item, i), 3000);
    end;
end;
