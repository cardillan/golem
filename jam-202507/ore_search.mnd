#set target = 8;
#set syntax = strict;

const MAX_DISTANCE = 100;

module ore_search;

remote


remote void searchForOres(core, dirx, diry)
    var corex = core.@x;
    var corey = core.@y;

    for main in 2 .. MAX_DISTANCE do
        for side in -main .. main do

        end;
    end
end;
