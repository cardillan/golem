linked display1;
linked switch1;
linked message1;
linked bank1;
linked bank2;
linked timer = processor1;
linked display = processor2;
linked oreLocator = processor3;
linked matrices = processor4;
linked blockBuilder = processor5;
linked polyController = processor6;
linked monoController = processor7;
linked flareController = processor8;
linked coreBuilder = processor9;
linked coreControllerConfig = processor10;
