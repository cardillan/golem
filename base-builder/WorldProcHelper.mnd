#set target = 8w;

#set program-name = "Base Builder - Helper";
#set program-version = "v0.4.0";

shard1.setprop(@copper, 300);
shard1.setprop(@lead, 90);
shard1.setprop(@graphite, 120);

while true do
    do while !switch1.enabled;

    switch1.enabled = false;

    for i in 0 ... 10 do
        shard1.setprop(lookup(:item, i), 3000);
    end;
end;
